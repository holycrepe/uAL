<UserControl x:Class="wUAL.UserControls.QueueToggleSettingControl "
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:my="clr-namespace:wUAL"
             xmlns:local="clr-namespace:wUAL.UserControls"                      
             xmlns:bind="clr-namespace:wUAL.Properties.Settings.Binding"
             mc:Ignorable="d"
             d:DataContext="{bind:Toggle Monitor}"
             d:DesignHeight="50" d:DesignWidth="300">
    <UniformGrid
		Columns="3"
        DataContext="{Binding DataContext, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type local:QueueToggleSettingControl}}}">
        <CheckBox
			IsChecked="{Binding All}"
			Content="All" />
        <CheckBox
			IsChecked="{Binding Torrents}"
			Content="Torrents"
			IsEnabled="{Binding All, Converter={my:InverseConverter}, Mode=OneWay}" />
        <CheckBox
			IsChecked="{Binding Metalinks}"
			Content="Metalinks"
			IsEnabled="{Binding All, Converter={my:InverseConverter}, Mode=OneWay}" />
    </UniformGrid>

</UserControl>
